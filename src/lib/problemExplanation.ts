/** Translate observations, not guesses about the root cause. Raw evidence stays in storage. */
export function explainProblem(error?: string | null, httpCode?: number | null) {
  const raw = error || "";
  if (/ลิงก์หลักและลิงก์สำรองใช้ไม่ได้/.test(raw)) return { reason: "รอบตรวจนี้เปิดไม่ได้ทั้งลิงก์หลักและลิงก์สำรอง ลูกค้าอาจเข้าใช้งานไม่ได้ผ่านเส้นทางที่ตรวจ", next: "ลองเปิดทั้งสองลิงก์จากเครือข่ายเดียวกัน และแจ้งผู้รับผิดชอบเตรียมลิงก์ทดแทนโดยเร็ว" };
  if (/หน้าปิดกั้น|block.?page/i.test(raw) || httpCode === 451) return { reason: "ผลตรวจพบหน้าที่แจ้งจำกัดการเข้าถึง แทนหน้าเว็บไซต์ที่ต้องการ", next: "ตรวจหน้าปลายทางจากเครือข่ายเดียวกันและทดสอบลิงก์สำรอง ให้ไอทีตรวจยืนยันว่าใครเป็นผู้จำกัดการเข้าถึง" };
  if (/UnknownHost|ENOTFOUND|EAI_AGAIN|Unable to resolve|No address associated/i.test(raw)) return { reason: "เครื่องตรวจหาที่อยู่ของเว็บไซต์นี้ไม่พบ จึงเปิดลิงก์หลักไม่ได้จากเครือข่ายที่ใช้ตรวจ", next: "ตรวจว่าพิมพ์ลิงก์ถูกต้อง แล้วลองเปิดผ่านเครือข่ายเดียวกันและตรวจลิงก์สำรอง ยังสรุปไม่ได้ว่าเว็บปิดหรือถูกบล็อก" };
  if (/Cleartext.*not permitted/i.test(raw)) return { reason: "แอปตรวจไม่อนุญาตให้เปิดลิงก์แบบไม่เข้ารหัส จึงตรวจหน้านี้ไม่สำเร็จ ไม่ได้ยืนยันว่าเว็บไซต์ล่ม", next: "ให้ไอทีตรวจลิงก์และการส่งต่อไปหน้า http:// รวมถึงการตั้งค่าของแอปตรวจ" };
  if (/timeout|timed out|หมดเวลา|ตอบกลับช้า/i.test(raw)) return { reason: "เว็บไซต์ตอบกลับไม่ทันเวลาที่กำหนด อาจเป็นเพราะเว็บหรืออินเทอร์เน็ตช้า ยังยืนยันไม่ได้ว่าเว็บล่ม", next: "ลองเปิดจากเครือข่ายเดิมและรอผลตรวจรอบถัดไป หากยังช้าให้ตรวจลิงก์สำรอง" };
  if (/SSL|TLS|certificate|handshake/i.test(raw)) return { reason: "เครื่องตรวจเชื่อมต่อเว็บไซต์แบบปลอดภัยไม่สำเร็จ", next: "ให้ไอทีตรวจใบรับรองความปลอดภัยของเว็บและวันเวลาบนมือถือ ไม่ควรข้ามคำเตือนความปลอดภัย" };
  if (/unauthorized/i.test(raw) || httpCode === 401) return { reason: "ปลายทางไม่อนุญาตให้เข้าถึงโดยไม่มีการยืนยันตัวตน", next: "ตรวจว่าข้อความมาจากแอปตรวจหรือเว็บไซต์ หากเป็นแอปตรวจให้ผู้ดูแลตรวจการผูกเครื่อง หากเป็นเว็บให้ตรวจว่าต้องล็อกอินหรือไม่" };
  if (httpCode === 403) return { reason: "เว็บไซต์ปฏิเสธการเข้าถึงจากเครื่องตรวจ ยังไม่ได้ยืนยันว่าเป็นการบล็อกของค่ายมือถือ", next: "ลองเปิดด้วยตนเองผ่านเครือข่ายเดียวกัน แล้วให้ไอทีตรวจสิทธิ์หรือระบบป้องกันบอทของเว็บไซต์" };
  if (httpCode === 404) return { reason: "เว็บไซต์ตอบกลับว่าไม่พบหน้าตามลิงก์นี้", next: "ตรวจ URL ว่าถูกต้องหรือมีการย้ายหน้า แล้วแก้เป็นลิงก์ที่เปิดได้จริง" };
  if (httpCode === 429) return { reason: "เว็บไซต์จำกัดจำนวนครั้งที่เข้าตรวจ จึงยังไม่ให้เปิดในรอบนี้", next: "รอตรวจใหม่และให้ไอทีตรวจความถี่ในการเช็ก ไม่ควรเพิ่มการตรวจถี่ขึ้น" };
  if (httpCode && httpCode >= 500) return { reason: "ระบบของเว็บไซต์ตอบกลับว่ามีข้อผิดพลาดในขณะตรวจ", next: "ตรวจลิงก์สำรองและแจ้งผู้ดูแลเว็บไซต์ พร้อมรอผลตรวจยืนยันอีกครั้ง" };
  if (/refused|ECONNRESET|reset|ConnectException|unreachable|network is unreachable/i.test(raw)) return { reason: "เครื่องตรวจเชื่อมต่อเว็บไซต์ไม่สำเร็จ หรือการเชื่อมต่อถูกตัดระหว่างทาง", next: "ตรวจอินเทอร์เน็ตของเครื่องและลองเปิดลิงก์ผ่านเครือข่ายเดียวกัน ก่อนสรุปว่าเป็นปัญหาที่เว็บไซต์" };
  return { reason: "เครื่องตรวจพบปัญหาระหว่างเปิดลิงก์ แต่ข้อมูลที่มียังระบุสาเหตุแน่ชัดไม่ได้", next: "ลองเปิดลิงก์จากเครือข่ายเดียวกับเครื่องตรวจ หากยังเข้าไม่ได้ ให้ตรวจลิงก์สำรองและส่งรายละเอียดให้ไอที" };
}
